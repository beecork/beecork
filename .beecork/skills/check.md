Run `npm run typecheck`. If it reports errors, read them, fix the cause, and re-run until it passes. Then run `npm run build` to confirm the bundle compiles.

Report concisely what you found and what you changed — do not paste full command output.
